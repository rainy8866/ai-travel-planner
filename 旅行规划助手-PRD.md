# AI 旅行助手 — 产品需求文档（PRD）

> 求职作品 · AI 产品经理实习 · 零经验个人可落地版本
> 版本：v1.7 · 文档语言：中文
> 核心一句话：**候选景点来自高德真实 POI（治幻觉），路线来自聚类+排序算法（治绕路），编排引擎按开放时间与时间预算硬约束排布（治时段空闲），Phase 1.5 用同一套编排规则跨天重分配 dropped（治"空闲却不排"），大模型只做结构化编排（不新增景点），提示词精简为「城市+偏好」（不喂天数/节奏/时段），所有标签加 confirm_keywords 白名单防噪声，子地点黑名单过滤（游客中心/检票口等），地图数字序号标记 + 地名常驻 + 按天切换高亮，第 n+1 天起点锚定第 n 天终点最近邻。**

---

## 版本变更记录

### v1.6 → v1.7

| 变更项 | 说明 |
|--------|------|
| Phase 1.5 重写（编排规则统一） | 原 Phase 1.5 用"cursor+半套条件"直接往其他天塞POI，与 Phase 1 的 orchestrate_day 不一致，导致"第1天有空闲但 dropped POI 塞不进"的 silent bug。v1.7 改为：收集所有天 dropped → 按"天空闲时长倒序"遍历 → 对候选天 `orchestrate_day(已排POI + [当前p])` 做完整试验 → 若 p 进入某时段而非 dropped 则接受替换，保证 Phase 1 与 1.5 使用同一套硬约束 |
| UI 提示移除（降低规则透传） | 删除输入页"按当前时段：每天约 X 小时游玩预算 · 共 N 天 ≈ XX 小时"、结果页"本次行程由规则引擎编排（含开放时间硬过滤 + 24h 上限 + 评分优先保留）"两句规则提示，避免暴露实现细节给用户 |
| 全天占用标记统一为「⬅」 | UI 侧「全天景点占用」标注改为简洁符号「⬅」，表示该时段被左侧全天 POI 占用 |
| 聚类分天降级日志移除 | 删除 `cluster_days` 兜底逻辑中"超出时间预算已强制并入"的 `log_error`，未分配 POI 由编辑引擎处理，分天阶段不再触发降级告警 |

### v1.5 → v1.6

| 变更项 | 说明 |
|--------|------|
| 跨天最近邻强锚定 | `cluster_days` 每天内部先用 `order_day_optimized()` 按贪心最近邻排好访问顺序，再以**排好序后的最后一个 POI** 作为下一天种子参考锚点，确保第 n+1 天起点真正离第 n 天终点最近（原实现`last_end_poi = current[-1]`指向的是填充过程的任意位置，导致跨天相邻失效） |
| 移除聚类分天降级日志 | 删除"超出时间预算已强制并入"的 `log_error` 调用——未分配的 POI 交由 编辑引擎 Phase 1.5 跨天重分配处理，不再在分天阶段打降级日志 |
| 全天占用简洁标注 | 跨时段 POI 覆盖的后续时段空标记由「（全天景点占用）」改为「⬅」，界面更简洁 |

### v1.4 → v1.5

| 变更项 | 说明 |
|--------|------|
| Cursor 溢出修复 | 新增 `_advance_cursor()`，cursor capped at soft_cap，避免 POI 在时段边界放入后 cursor 溢出导致后续 POI 塞不进（如成都博物馆 180min 恰在 12:00 边界放入，cursor 变 12:15 超过 soft_cap，阻塞后续 POI） |
| 全天 POI 跨时段 | duration≥480min 的 POI（如青城山 8 小时）允许跨连续时段排列（morning→afternoon），解决单时段无法容纳超长 POI |
| 全天 POI 独占一天 | `cluster_days` 中种子 POI≥480min 时独占一天，不再贪心填充其他 POI |
| 移除 full_day_exclusive | 全天 POI 不再阻塞其他时段的普通 POI，只标记已占用的时段 |
| Phase 1.5 跨天重分配 | `render_itinerary` 新增重分配逻辑：POI 在某天排不下时，自动尝试放到其他天的空闲时段 |
| 开放时间硬约束 | 编辑引擎三层检查 `open_time`：POI 结束时间必须≤开放上限，不满足则跳过该时段（解决武侯祠 08:30-18:30 被排到晚上） |
| parse_open_range 迁移 | 开放时间解析函数从 render/routing 移至 `_config/config.py`，解决循环导入 |
| 时段覆盖优先 | `cluster_days` 新增 `_covered_slots()`，优先选择能覆盖未覆盖时段的 POI，确保每天尽量覆盖所有选中时段 |
| dropped 汇总显示 | 所有未排入景点在最后一天后统一显示，不再分天显示 |
| 全天 POI 空闲标注 | 跨时段 POI 的后续时段显示「（全天景点占用）」而非「（空闲）」 |
| POI 显示时间范围 | 行程中每个 POI 显示 time_range，便于用户理解全天 POI 的占用情况 |
| 运行状态默认折叠 | 「运行状态」区块改为默认折叠的 expander，隐藏降级日志 |
| 移除编排丢弃降级日志 | 删除"编排丢弃"的 `log_error` 调用 |
| schedule_one_day 跨时段 | 允许 `schedule_one_day` 显示跨时段 POI，不再因 actual_end > soft_cap 跳过 |

### v1.3 → v1.4

| 变更项 | 说明 |
|--------|------|
| 删除"美食探店"标签 | 从 `INTEREST_TAGS` 配置整体移除 |
| 新增"主题乐园"标签 | 新增主题乐园偏好，含 confirm_keywords 白名单 + 安全库 POI |
| 高德反查去掉类型码过滤 | 高德反查只验证"店名在该城市是否真实存在"，不再用类型码过滤；改由 confirm_keywords 白名单 + 子地点黑名单双重过滤 |
| 子地点黑名单过滤 | 新增 `_is_sub_place()`，过滤名字含"游客中心/检票口/出口/停车场/售票处/东门/南门/北门/西门"等关键词的 POI |
| 所有标签加 confirm_keywords | 给全部 7 个标签补 confirm_keywords 白名单 |
| 提示词精简为「城市+偏好」 | LLM 提示词删除天数/节奏/时段，只保留城市和用户选的偏好标签；每个标签输出 12 个店名 |
| 新增出游时间段多选 | 输入页新增"出游时间段"多选（上午/下午/晚上），默认不预选，必须至少选 1 个；容量公式 = 时段数 × 每时段数 |
| 地图数字序号标记 | 用 folium.DivIcon 渲染彩色圆形标记，圆内白色数字表示访问顺序 |
| 地图地名常驻 | Marker 旁用 Tooltip 常驻显示地点名称，不用点击 |
| 地图按天切换高亮 | 结果页下拉框选择"第几天"，当天原色+路线实线，其他天灰色+虚线 |
| 结果页下拉选择天数 | 用 st.selectbox 下拉选择"第几天" |
| 超量提示 | 聚类前计算容量，超量时 st.warning 弹窗告知 |

### v1.2 → v1.3

| 变更项 | 说明 |
|--------|------|
| 输入页无默认值 | 城市/兴趣标签不再有默认值，改为占位提示 + 空值轻量校验 warning |
| 删除"亲子乐园"标签 | 从 `INTEREST_TAGS` 配置整体移除 |
| "无高德 Key"文案区分 | 候选为空时先判断 Key 是否真缺失，再给出不同提示文案 |
| 地图底图换高德公开矢量瓦片 | 默认底图改为高德公开矢量瓦片（免 Key、国内秒连、中文注记齐全）；卫星影像/OSM 为可选图层；天地图仅在瓦片真 HTTP 200 时加入 |
| 根因 403/超时灰底修复 | 明确灰底 = 天地图 Key 类型错 403 + OSM 国内超时双链路同时失败 |

### v1.1 → v1.2

| 变更项 | 说明 |
|--------|------|
| 用户输入字段瘦身 | 删除"预算档位/同行人数/出行方式/心愿清单"等未实现字段，保留【城市、天数、节奏、兴趣标签】 |
| 结果页输出瘦身 | 删除午餐/晚餐段渲染，移除 food_pool 全链路，仅输出游玩 POI |
| 提示词对齐方案 A | Step1+Step3 两段 Prompt 完整接入用户输入，显式输出 LLM/规则引擎模式提示 |
| 防跨城污染 | 兜底数据严格按城市映射，绝不拿其它城市数据冒充 |
| 标签防噪声白名单 | 新增 confirm_keywords 验证层，剔除挂羊头卖狗肉噪声 |
| 日志单次性 + 分组 | 每次生成前清空日志；运行状态按 (module, step, type, degraded) 分组汇总 |
| 目录结构 | 统一字母开头模块名，删除 0X_ 前缀 |
| 运行状态模式提示 | 显式提示"LLM 是否实际执行" |

---

## 目录

1. [项目背景与定位](#1-项目背景与定位)
2. [需求目标](#2-需求目标)
3. [用户画像与核心场景](#3-用户画像与核心场景)
4. [整体业务流程与系统架构](#4-整体业务流程与系统架构)
5. [功能需求详情](#5-功能需求详情)
6. [技术选型与决策](#6-技术选型与决策)
7. [路线规划算法详解](#7-路线规划算法详解)
8. [规则引擎约束明细](#8-规则引擎约束明细)
9. [异常兜底与降级链路](#9-异常兜底与降级链路)
10. [页面设计与交互规范](#10-页面设计与交互规范)
11. [可运行 MVP 的目录结构与关键代码思路](#11-可运行-mvp-的目录结构与关键代码思路)
12. [部署方案](#12-部署方案)

---

## 1. 项目背景与定位

### 1.1 问题来源

现阶段的 AI 旅行规划产品（对话式攻略、大模型一键生成行程）存在四个被反复吐槽的真实问题：

1. **编造景点 / 跨城污染**：纯大模型很容易生成现实中不存在的地点，或把真实景点张冠李戴到错误城市；更隐蔽的是"把杭州 in77 塞给成都"这种跨城污染。
2. **地理乱跳**：生成的行程 A→B→C 在真实地图上可能横跨大半个城市，每天来回折返，通勤时间被严重低估。
3. **提示词形同虚设**：虽然写了 Prompt，但用户输入的【天数/节奏/城市/标签】没有完整传进 Prompt，导致"选了紧凑依旧出 1+1、选了成都出杭州数据"。
4. **时段编排不合理**：即便候选点正确、地理顺路，排入行程时仍可能出现"某时段明明空闲但用户选的景点排不进去"、"开放时间不满的景点（如武侯祠晚上不开放）被排到晚上"、"全天景点（如青城山 8h）无处安放"等编排层面的 bug。

这四类问题指向同一个根因：**让大模型既当"信息来源"又当"路线规划器"，不把用户输入完整喂给它，且编排层缺少开放时间/时间预算的硬约束**。

### 1.2 本项目定位

本项目把它拆成五条分工明确的链路，分别用最擅长的方式解决：

| 环节 | 负责方 | 目标 |
|------|--------|------|
| 候选景点获取 | 高德 POI 接口（真实数据）+ 城市级安全库兜底 + confirm_keywords 白名单防噪声 | 从源头杜绝编造景点、杜绝跨城污染、杜绝挂羊头卖狗肉 |
| 通勤耗时计算 | Haversine 直线距离（不调距离矩阵 API，避免 INVALID_PARAMS 报错） | 不依赖外部接口也能给出相对合理的排序依据 |
| 路线排布优化 | 地理聚类 + 贪心最近邻，每天内部排好序后跨天最近邻锚定 | 减少跨区折返，且第 n+1 天起点离第 n 天终点最近 |
| 行程编排 | 编辑引擎（Cursor 约束 + 全天跨时段 + 开放时间硬约束 + 跨天重分配）+ 规则引擎兜底 | 把用户选择的 POI 按开放时间与时间预算排进各时段，无空闲无 dropped |
| 电子地图底图 | 默认 = 高德公开矢量瓦片（免 Key、国内秒连、中文注记）；可选 = 卫星影像、天地图（瓦片真 200 才启用）、OSM | **保证"地图必定显示真实电子地图背景"** |

一句话定位：**"真实数据 + 约束算法 + 提示词完全对齐用户输入 + 编排引擎硬约束 + 底图必定显示真实电子地图"的 AI 旅行路线规划助手可运行 MVP**，同时作为 AI 产品经理求职的完整作品集项目。

---

## 2. 需求目标

### 2.1 定性目标

- 建立"候选景点必须来自地图真实 POI，或来自**该城市**的城市级安全库兜底"的硬约束，从机制上杜绝编造与跨城污染；
- 行程每天活动范围尽量收敛在同一地理片区，第 n+1 天起点离第 n 天终点最近，减少跨区往返；
- 编排引擎按**开放时间 + 时间预算**硬约束排布，保证每天各选中时段尽量有景可排、排不进用户选项时跨天重分配；
- 大模型两段 Prompt 都完整对齐用户输入字段；
- 结果页显式告诉用户"这次 LLM/提示词到底有没有被执行"。

### 2.2 可量化指标（MVP 演示口径）

| 指标 | 目标值 |
|------|--------|
| 候选景点均为高德真实 POI 或该城市安全库 | 100% |
| 大模型输出中"不在候选池内"的景点数 | 0（硬性约束） |
| 开放时间不满足的时段不排入 POI（如武侯祠晚上） | 100% |
| 用户选择的 POI 优先排入，未排入的（dropped）统一汇总显示 | dropped 率尽量低；有 dropped 时不再补推荐 |
| API 调用失败时自动降级 | 自动切换，无需人工介入 |
| 节点边界不因 Cursor 溢出而静默丢点 | 100% |
| 地图底图显示真实电子瓦片（非灰色） | 100% |

---

## 3. 用户画像与核心场景

### 3.1 目标用户

| 画像 | 使用频次 | 痛点强度 | 关键需求 |
|------|----------|----------|----------|
| 周末两天游的城市白领 | 月度 | 高 | 快、省心、不绕路、偏好标签精准 |
| 中转游客（1-3 天） | 季度 | 高 | 片区集中、通勤少、节奏可选 |
| 穷游/自由行学生 | 月度 | 中 | 标签覆盖小众偏好，不被普通店污染 |
| "没耐心用户" | 月度 | 极高 | 三页流程、不用改 Prompt、明确提示哪步走 LLM/哪步降级 |

### 3.2 核心使用场景

1. **P0 经典场景**：用户输"成都 · 3 天 · 节奏紧凑 · 二次元+自然风光"→ 候选池出现真正成都本地二次元地标，不出现跨城污染；结果页显示 `mode=llm`。
2. **P0 防幻觉演示**：输入不在白名单里的新城市且无高德 Key，明确降级"仅覆盖 9 城市内置候选"，绝不拿杭州示例冒充。
3. **P0 开放时间演示**：选武侯祠（08:30-18:30），即使晚上时段空闲也**不会被排到晚上**。
4. **P0 全天景点演示**：选青城山（8h），允许跨 morning+afternoon 连续排列，占用的后续时段显示"⬅"而非"（空闲）"。
5. **P1 降级演示**：手动删 DEEPSEEK_KEY，结果页变蓝提示"本次走规则引擎，LLM 未执行"。

---

## 4. 整体业务流程与系统架构

### 4.1 端到端业务链路

```
用户输入（5 字段）
  城市 + 天数 + 节奏 + 兴趣标签 + 出游时间段（早/中/晚）
    │
    ▼
Step1  Prompt（城市+偏好）→ DeepSeek 生成各标签 12 个候选店名
    │
    ▼
Step2  三路并行注入：安全库 / LLM店名→高德反查验证 / 关键词兜底
    │
    ▼
Step3  双重过滤：confirm_keywords 白名单 + 子地点黑名单
    │
    ▼
Step4  三层去重（精确名 → 坐标 → 同标签相似度）
    │
    ▼
Step5  用户勾选候选 → 容量校验（超量 warning）→ 聚类分天 → 单日最近邻排序
    │    · ≥480min 长时段 POI 独占一天
    │    · 优先覆盖所有选中时段（_covered_slots）
    │    · 每天内部排好序后，跨天锚定下一天起点
    ▼
Step6  行程编排（编辑引擎）：
  ├── Phase 0: 预解析所有 POI 的 open_time → ot_rng
  ├── Phase 1: 按天编排（orchestrate_day，按评分降序）
  │    · Cursor cap at soft_cap（_advance_cursor）
  │    · 全天 POI（≥480min）跨连续时段放置
  │    · 同簇 POI 连续时段排列（地理聚类）
  │    · open_time 硬约束：结束时间≤开放上限
  ├── Phase 1.5: 跨天重分配
  │    · dropped POI 尝试放到其他天的空闲时段
  │    · 跨天去重：已排入 POI 不再重复
  ├── Phase 2: 只有全部用户 POI 都排入才补推荐
  ├── Phase 3: schedule_one_day 生成时间轴（允许跨时段 POI 显示）
  │
    ▼
输出  三页：输入 → 候选勾选 → 结果（地图+数字序号标记+地名常驻+按天高亮+分日时间轴+dropped汇总显示+运行状态折叠）
```

### 4.2 关键设计原则

> **信息来源 ↔ 决策引擎 ↔ 编排引擎 ↔ 呈现层严格分离**：地图接口/安全库负责"什么是真实且属于该城市的"，算法负责"怎么排最优"，编辑引擎负责"怎么在时间轴上放入各个时段"，大模型只负责"怎么按用户节奏拆段呈现"——任何环节无权新增景点，也不能拿别的城市假数据冒充。

> **编辑引擎硬约束优先**：比"排得漂亮"更重要的是"排得合法"——开放时间不满的时段绝不放入，Caps 不因 cursor 溢出静默丢点，全天景点能找到容身之处，排不下的用户选项跨天重分配而非直接丢弃。

> **提示词精简为「城市+偏好」**：Step1 只给 LLM 城市和用户选的标签，不喂天数/节奏/时段（这些是后续编排阶段的事），让 LLM 专注于"该城市有哪些真正的热门景点"。

---

## 5. 功能需求详情

### 模块一：候选游玩项目获取与筛选

#### 实现过程

1. **入参解析**：读取用户输入 5 字段 `city / days / pace / tags / periods`。
2. **Prompt 生成店名**：`generate_candidates(city, tags)` 二参签名，Prompt 仅含城市+偏好，每个标签输出 12 个店名，覆盖不同区县。
3. **三路并行注入**：
   - 来源A 安全库：按标签从 `SAFE_POI_LIBRARY` / `CITY_SAFE_POI_LIBRARY` 注入；
   - 来源B LLM反查：店名 → 高德 `place/text` 反查验证存在性（不做类型码过滤）；
   - 来源C 关键词兜底：A+B 不足时用 `search_keywords` 直接搜高德补齐。
4. **双重过滤**：confirm_keywords 白名单（名字+type 必须命中）+ 子地点黑名单（游客中心/检票口/出口/停车场/东门/南门等）。
5. **三层去重**：精确名 → 坐标（5 位小数）→ 同标签相似度（≥0.60 丢弃）。
6. **城市级兜底**：严格按城市映射安全库，未覆盖城市返回空+降级日志，不用他城数据冒充。

#### 当前支持的 7 个标签

| 标签 | confirm_keywords（白名单） | 典型过滤场景 |
|------|---------------------------|-------------|
| 自然风光 | 公园、湿地、湖、山、森林、风景区、植物园... | 过滤"花店/苗圃" |
| 人文历史 | 博物馆、历史街区、故居、寺、塔、阁、祠、庙... | 过滤"文创店/纪念品店" |
| 主题乐园 | 主题乐园、游乐园、乐园、海洋公园、宋城、欢乐谷... | 过滤普通商业体 |
| 购物商圈 | 购物中心、商场、百货、银泰、万象、in77... | 过滤"花店/服装店" |
| 艺术展馆 | 美术馆、艺术馆、展览馆、剧院、画廊... | 过滤"画材店/文具店" |
| 二次元 | 动漫、二次元、ACG、谷子、手办... | 过滤"普通咖啡店/服装店" |

---

### 模块二：候选勾选（选择页）

1. 分组折叠展示候选（按用户勾选的标签），每个标签不默认勾选候选（完全由用户决定）；已下架标签不展示。
2. 容量校验：`max_poi_per_day = len(periods) × SPOTS_PER_PERIOD[pace]`，超量时 `st.warning` 弹窗。
3. 候选为空时文案区分：Key 真缺失 → "请注册高德 Key"；Key 正常但候选不足 → "安全库未覆盖该城市×该标签，建议换城市或标签"。

#### 关键规则

- 容量公式：悠闲×3时段=3/天，正常×3时段=6/天，紧凑×3时段=9/天。
- 超量只给 warning 不阻止继续，后续聚类会强制并入最宽松的天。

---

### 模块三：聚类分片 + 跨天最近邻排序

略（区县粗分 + 网格细分；贪心最近邻 + 随机起点；`order_day_optimized` 每天排好序后作为下一天起点锚点）。

---

### 模块四：编辑引擎行程编排（Route 编排）

#### 输入

- 已聚类的分天 POI 列表；
- 用户选中时段 periods（早/午/晚）；
- 各 POI 的 open_time 与 duration。

#### Phase 0：预解析

所有 POI 的 open_time 解析为 `ot_rng` 元组，空开放时间视为全时段可排。

#### Phase 1：按天编排（orchestrate_day，评分降序）

- 逐 POI 按评分降序尝试放入时段；
- **Cursor cap at soft_cap**：POI 放入后 cursor 不越过时段 soft_cap，避免"看似已满"阻塞；`_advance_cursor()` 处理边界溢出；
- **全天 POI 跨时段**：duration≥480min 的 POI 允许跨连续时段放置（morning→afternoon）；
- **同簇 POI 连续时段**：地理同簇 POI 优先排进相邻时段；
- **open_time 硬约束**：`actual_end ≤ ot_rng[1]` 才放行，否则跳过该时段（兜底逻辑同样检查，杜绝武侯祠晚上场景）。

#### Phase 1.5：跨天重分配

- 某天排不下的 POI，遍历其他天的空闲时段尝试放入；
- 跨天去重，已排入的 POI 不重复。

#### Phase 2：补推荐门控

- **仅当全部用户 POI 都排入**时才补充推荐景点；存在 dropped 时禁止补推荐，保证用户选择优先。

#### Phase 3：时间轴生成（schedule_one_day）

- 允许跨时段 POI 显示，不在 actual_end > soft_cap 时跳过。

#### 反向校验

`allowed` 集合反向校验，输出严禁出现候选池外景点。

---

### 模块五：地图与结果展示

- 时间轴只渲染 `kind="poi"`，无 meal；
- 结果页顶部显式提示 mode=llm（绿）或 mode=rules（蓝）；
- 默认底图 = 高德公开矢量瓦片（免 Key、国内秒连、中文注记）；可选卫星影像/OSM/天地图（探测真 200 才启用）；
- 数字序号标记 + 地名常驻 Tooltip + 按天切换高亮 + 超量提示。

---

### 模块六：运行状态

- `reset_logs()` 每次生成前清空日志；
- 按 `(module, step, type, degraded)` 分组汇总，同类合并只给最后一条 msg + 最新 3 行 raw；
- 「运行状态」区块默认折叠为 expander，隐藏降级日志，不干扰用户视图。

---

## 6. 技术选型与决策

| 决策点 | 方案 | 理由 |
|--------|------|------|
| 框架 | Python + Streamlit | 三页状态机简单、演示友好 |
| 候选生成 | DeepSeek（Step1）+ 高德 POI 反查 + 安全库兜底 | 真实数据治幻觉 |
| 距离 | Haversine 直线距离 | 不依赖外部 API，避免 INVALID_PARAMS |
| 地图 | folium + 高德公开矢量瓦片 | 免 Key、国内秒连、中文注记 |
| 编排 | 编辑引擎（硬约束）+ 规则引擎兜底 | 开放时间/时间预算硬约束，防静默丢点 |

---

## 7. 路线规划算法详解

- **地理聚类**：区县粗分 + 网格细分，同片区 POI 归一簇。
- **单日排序**：贪心最近邻（`order_day_optimized`），随机起点多次取更短路线。
- **跨天锚定**：第 n 天内部排好序后，以最后一个 POI 为锚点，第 n+1 天种子选离它最近的剩余 POI，并用 `_covered_slots()` 优先补未覆盖时段。
- **时段填充**：按评分降序 + 同簇连续，cursor 不越 soft_cap，全天 POI 跨时段，开放时间硬约束，排不下跨天重分配。

---

## 8. 规则引擎约束明细

- 时段定义：morning(08:00-12:00)、afternoon(12:00-18:00)、evening(18:00-22:00)。
- 容量：每时段 POI 数按节奏：悠闲1/正常2/紧凑3。
- 开放时间空 → 全时段可排；开放时间非空 → 结束时间必须≤开放上限。
- 全天 POI（≥480min）跨连续时段放置，占用后续时段标记"⬅"。

---

## 9. 异常兜底与降级链路

| 场景 | 兜底 |
|------|------|
| DEEPSEEK 不可用 | 走规则引擎（mode=rules），结果页显式提示，节奏对齐 |
| 高德 Key 缺失 | 走城市安全库；仍为空则提示"注册 Key" |
| 候选不足 | 安全库未覆盖时提示"换城市/标签" |
| POI 某天排不下 | Phase 1.5 跨天重分配 |
| CLI/STEP 日志 | reset_logs 单次性 + 分组折叠 |

---

## 10. 页面设计与交互规范

### 10.1 页面流转

```
[输入页] → [候选选择页] → [结果页]
```

三页均为 Streamlit 三态状态机（`st.session_state["step"]`）。

### 10.2 输入页

1. 目的地（城市）：空输入框 + 占位「例如：杭州 / 成都 / 上海」。
2. 行程天数（1-7）：默认 1。
3. 出行节奏（悠闲/正常/紧凑）：默认 normal。
4. **出游时间段多选**：上午/下午/晚上，默认空，必须至少选 1 个。
5. 兴趣标签多选：7 个标签，默认空。
6. 生成按钮 + 轻量校验：城市空 / 未选时段 / 未选标签 → warning。

### 10.3 选择页

- 按标签分组折叠，multiselect 默认空，显示"已选 X/总 N"；
- 容量提示 + 超量 warning；
- "确认选择，生成行程"按钮。

### 10.4 结果页

**顶部**：LLM/规则引擎模式提示 + 运行状态折叠。

**地图（左侧）**：默认高德公开矢量瓦片；下拉切换第几天；数字序号标记；地名常驻 Tooltip；按天高亮。

**行程（右侧）**：分日时间轴（仅 poi），每天按时段分 morning/afternoon/evening 展示；跨时段全天 POI 的后续时段空标记"⬅"；排不下的景点在最后一天后汇总显示。

---

## 11. 可运行 MVP 的目录结构与关键代码思路

```
旅行规划助手/
├── app.py                 # 入口：三页状态机（输入→选择→结果）
├── requirements.txt       # 依赖（folium / requests / python-dotenv / streamlit）
├── .env / .env.example    # AMAP_KEY / DEEPSEEK_KEY
├── ui/                    # UI 层
│   ├── input_page.py      #   输入页（5 字段）
│   ├── selection_page.py  #   选择页（标签分组、容量提示）
│   └── result_page.py     #   结果页（地图、按天切换、dropped 汇总）
├── _config/
│   ├── config.py          #   API key、容量公式、parse_open_range、高德瓦片端点
│   ├── config_data.py     #   INTEREST_TAGS（7 标签）、SAFE_POI_LIBRARY
│   └── log.py             #   log_error + reset_logs + read_logs
├── poi_pipeline/          # 候选生成与验证
│   ├── llm.py             #   generate_candidates(city, tags)
│   ├── amap.py            #   高德 POI 搜索 + is_same_poi
│   └── pipeline.py        #   build_candidates + confirm_keywords + 子地点黑名单
├── selection/             # 选择逻辑
├── routing/               # Haversine + 贪心 + 地理聚类 + 跨天锚定
│   └── routing.py
├── rules/                 # 规则引擎
├── render/                # 编辑引擎（编排）
│   └── render.py
├── map_view/              # 地图渲染
└── fallback/              # 异常兜底
```

### 关键函数清单

| 函数 | 位置 | 说明 |
|------|------|------|
| `build_candidates(ui)` | `poi_pipeline/pipeline.py` | 候选主入口 |
| `generate_candidates(city, tags)` | `poi_pipeline/llm.py` | Step1 出店名，仅城市+偏好 |
| `_is_sub_place` | `poi_pipeline/pipeline.py` | 子地点黑名单过滤 |
| `_tag_confirms` | `poi_pipeline/pipeline.py` | confirm_keywords 白名单过滤 |
| `cluster_days` | `routing/routing.py` | 分天 + 时段覆盖优先 + ≥480min 独占一天 + 跨天锚定 |
| `order_day_optimized` | `routing/routing.py` | 单日贪心最近邻排序 |
| `_covered_slots` | `routing/routing.py` | 计算每天已覆盖时段集合 |
| `render_itinerary` | `render/render.py` | 编排主入口（Phase 0/1/1.5/2/3） |
| `orchestrate_day` | `render/render.py` | 单日编排（跨时段/开放时间硬约束/cursor cap） |
| `_advance_cursor` | `render/render.py` | Cursor capped at soft_cap |
| `schedule_one_day` | `render/render.py` | 时间轴生成（允许跨时段 POI） |
| `parse_open_range` | `_config/config.py` | 统一开放时间解析 |
| `reset_logs` | `_config/log.py` | 清空日志 |

---

## 12. 部署方案

略（Streamlit Cloud / Docker / 本地三种）。

---

*（文档结束 · v1.6 · 候选真实化 + 编辑引擎硬约束 + 跨天最近邻锚定）*